<smil>
  <head/>
  <body>
    <switch>
      {% for video in videos %}
        <video src="mp4:path{{video['src']}}" system-bitrate="{{video['total_bitrate']}}" width="{{video['width']}}" height="{{video['height']}}" />
      {% endfor %}
    </switch>
  </body>
</smil>
